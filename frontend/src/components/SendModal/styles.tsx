import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  ticker: {
    fontWeight: "bold",
    fontSize: "16px",
    color: "#393939",
  },
  gridContainer: {
    width: "100%",
    height: "100%",
  },
  address: {
    width: "100%",
  },
});

export { useStyles };
